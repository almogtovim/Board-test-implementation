//
// Created by tovim on 21/03/2021.
//

#include "Board.hpp"

/*
if ((row > limit && dir == Direction::Vertical) || (col > limit && dir == Direction::Horizontal)){
throw "OutOfBoardException"
}
int row = (int)(rand() % MAX_VAL);
int col = (int)(rand() % MAX_VAL);
Direction dir;
int length = toCheck.length();
if ((int)rand() % 1 == 0) {
dir = ariel::Direction::Horizontal;
}
else {
dir = ariel::Direction::Vertical;
}
board.post(row, col, dir, toCheck);
return (board.read(row, col, dir, length).compare(toCheck) == 0);
 */